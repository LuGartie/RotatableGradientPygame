This is a function that creates a gradient (two colors) rectangle that can be rotated around a given axis (on center by default).

Esta é uma função que cria um retângulo com gradiente (duas cores) que pode ser rotacionado em torno de um eixo dado (no centro por padrão).